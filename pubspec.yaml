name: im_app
description: A new Flutter IM app.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"  # 升级 Dart SDK 版本以兼容最新依赖

dependencies:
  flutter:
    sdk: flutter

  # Firebase 核心依赖（2024年最新稳定版）
  firebase_core: ^2.32.0
  firebase_auth: ^4.18.0
  cloud_firestore: ^4.16.0

  # 状态管理
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0  # 添加代码规范检查

flutter:
  uses-material-design: true
